function solution(seoul) {
   //배열의 모든 요소를 처음부터 특정 요소 'kim'을 발견할 때 까지 차례대로 검색한다.
   //선형검색
   // 대문자 'K', ~있다. 온점 금지
    
    //indexOf 메서드로 seoul 배열에 'Kim' 요소가  존재하는지 확인한다.
    //요소가 존재하는 인덱스를 result 변수에 할당하여 출력한다.
     let result = seoul.indexOf('Kim')     
           return `김서방은 ${result}에 있다`
     
}